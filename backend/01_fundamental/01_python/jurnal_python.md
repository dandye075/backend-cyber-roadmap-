# Jurnal Belajar – Syntax Dasar Python

🗓️ Tanggal: 18 Desember 2025  
⏱️ Durasi: ±1 jam

## ✅ Sudah Dipelajari
- Input/output (`print`, `input`)
- Tipe data dasar (`int`, `str`, `bool`)
- Operator aritmatika (+, -, *, /)
- If/Else → pengambilan keputusan
- Looping → `for`, `while`
- Infinite loop → kenapa bisa terjadi dan cara menghentikan
  - `+= 1` dan `break` → menghentikan lewat program
  - `Ctrl+C` → menghentikan manual saat loop sudah berjalan
- Operator logika (`and`, `or`, `not`)

## ⏳ Belum Dipelajari (lanjut besok)
- Operator perbandingan (`==`, `!=`, `<`, `>`, `<=`, `>=`)
- Struktur data (List, Tuple, Dictionary, Set)
- OOP (Object-Oriented Programming)
- Modul & Package

**Tanggal:** 19 Desember 2025  

## List
- ✅ Sudah selesai: akses index, slicing (start, stop, step, index negatif), reverse dengan `[::-1]`.  
- Latihan: `angka[2:]`, `angka[1:6]`, `angka[::2]`, `angka[::-1]`.  
- Semua hasil benar, pola slicing sudah mantap.  

## Tuple
- ✅ Sudah selesai: definisi (immutable), cara membuat, akses index positif/negatif, slicing, fungsi bawaan (`len`, `sum`, `min`, `max`).  
- Latihan: `angka[2]`, `buah[-1]`, `angka[1:4]`, `len(hari)`, `sum(angka)`, `max(nilai)`.  
- Semua hasil benar, konsep immutable dipahami (error saat assignment).  

## ⏳ Belum Dipelajari (lanjut besok)
- Struktur data (dictionary, Set)
- OOP (Object-Oriented Programming)
- Modul & Package
